# elegso.site — полный паспорт проекта

> **Назначение документа**
>
> Этот файл является **единым источником истины** о проекте `elegso.site`.
> Он предназначен для:
> - быстрого восстановления полного контекста проекта в любом новом чате;
> - работы с сайтом через текстовые команды ("сделай", "измени", "переставь");
> - точного понимания, **какие файлы и где нужно править**;
> - безопасного и воспроизводимого внесения изменений.
>
> 📌 **ВАЖНОЕ ПРАВИЛО**: если структура проекта меняется — **этот README.md обязан быть обновлён**.

---

## 1. Общая информация о проекте

- **Проект**: ELEGSO.site
- **Тип**: статический сайт (Astro, output: static)
- **Назначение**: основной маркетингово‑представительский сайт юридической компании **ELEGSO**
- **Текущий публичный домен**: https://law.elegso.ru
- **Планируемый основной домен**: https://elegso.ru
- **Локальная разработка**: http://localhost:4321
- **Локально сайт лежит в папке на компьютере macbook: /Users/timchenko.t.v/Projects/elegso.site/

ВАЖНО: рабочая копия сайта ELEGSO находится ЛОКАЛЬНО на macOS.

Локальный путь проекта:
 /Users/timchenko.t.v/Projects/elegso.site/

Все команды по умолчанию:
- для macOS
- выполняются локально
- НЕ для сервера

Если нужен сервер — я укажу это явно.
Если путь отличается — сначала спроси, не предполагай.

Проект спроектирован так, чтобы:
- сайт можно было **менять словами**;
- тексты, блоки и страницы были **предсказуемо локализованы**;
- ИИ мог безопасно давать правки без «угадывания»;
- смена домена **не требовала переписывания кода** (используются root‑relative пути `/images/...`).

Если я пишу "сделай бэкап сайта" — это всегда означает:

- полный бэкап ВСЕГО проекта
- один архив
- имя архива содержит дату и время (YYYY-MM-DD_HHMMSS)
- никаких исключений без вопроса
- сразу выдавай рабочий скрипт под macOS

РЕЖИМ РАБОТЫ С КОМАНДАМИ (ОБЯЗАТЕЛЬНО):

1) Если ты даёшь мне команды/скрипты для выполнения — ты ОБЯЗАН:
   - после каждого шага давать команду ПРОВЕРКИ (verify)
   - требовать от меня прислать вывод проверки
   - по моему выводу проверки делать вывод: "ОК" или "Ошибка", и что делать дальше

2) НИКОГДА не давай много этапов сразу.
   - Только ОДИН шаг за сообщение: "ШАГ 1", затем команда выполнения + команда проверки.
   - Дальше ЖДЁШЬ мой вывод проверки.
   - Только после этого даёшь "ШАГ 2".

3) Если есть неопределённость (путь, версия, окружение, наличие утилит) —
   сначала дай команду ПРОВЕРКИ окружения, а не основную команду.

4) Каждая команда должна быть:
   - копипастабельной
   - безопасной (без удаления/перезаписи без предупреждения)
   - с понятным ожидаемым результатом проверки

5) Если где-то возможны варианты (разные пути/службы/OS) —
   сначала выяви правильный вариант проверкой, и только потом давай действие.

ФОРМАТ ОТВЕТА НА ЛЮБОЙ ТЕХНИЧЕСКИЙ ЗАПРОС:

ШАГ N — <короткое название>
Команда:
<одна команда или небольшой блок команд>
Проверка:
<команда проверки>
Ожидаю от тебя вывод проверки.

---

## 2. Технологический стек

- **Astro** — основной фреймворк (static output)
- **Node.js / npm** — установка зависимостей и сборка
- **HTML / Astro Components** — представление
- **CSS** — стили (global + legacy)
- **JSON / Markdown** — контент и мета‑описания
- **Git + GitHub** — контроль версий и доставка на сервер

Сайт **не использует CMS** — весь контент управляется через файлы и Git.

---

## 3. Корневая структура репозитория

```text
repo/
├── README.md              ← паспорт проекта (ЭТОТ ФАЙЛ)
├── brand.md               ← бренд, тон, стиль, визуальные правила
├── STRUCTURE.md           ← архитектурная логика проекта
├── site-map.json          ← карта страниц и якорей
├── astro.config.mjs       ← конфигурация Astro
├── package.json           ← зависимости и npm‑скрипты
├── src/
│   ├── pages/             ← страницы сайта
│   ├── components/        ← блоки и секции
│   ├── styles/            ← стили
│   └── content/           ← контент (JSON)
├── public/                ← статика (images, assets)
└── dist/                  ← результат сборки (НЕ редактируется)
```text
repo/
├── README.md              ← ЭТОТ ФАЙЛ (паспорт проекта)
├── brand.md               ← бренд, тон, стиль, визуальные правила
├── STRUCTURE.md           ← архитектурная логика проекта
├── site-map.json          ← карта страниц и якорей
├── astro.config.mjs       ← конфигурация Astro
├── package.json           ← зависимости и скрипты
├── src/
│   ├── pages/
│   ├── components/
│   ├── content/
│   └── layouts/
├── public/
│   └── images/
└── dist/                  ← результат сборки (не редактируется)
```

---

## 4. Папка `src/pages` — страницы сайта

```text
src/pages/
├── index.astro                 ← главная страница
├── cases/
│   ├── index.astro             ← список кейсов
│   └── [slug].astro            ← детальная страница кейса
```

### Принципы
- **1 файл = 1 страница**
- `index.astro` — точка входа сайта
- Страницы собираются из компонентов

📌 Если запрос:
- «измени главную» → `src/pages/index.astro`
- «измени список кейсов» → `src/pages/cases/index.astro`
- «измени страницу кейса» → `src/pages/cases/[slug].astro`

---

## 5. Папка `src/components` — блоки и секции

Примеры ключевых компонентов:

```text
src/components/
├── TeamNative.astro        ← блок команды
├── Header.astro
├── Footer.astro
└── *.astro
```

### Принципы
- **Один компонент = один логический блок**
- Компоненты:
  - не хранят бизнес‑логику
  - используют props и данные

📌 Если запрос:
- «измени отображение команды» → `TeamNative.astro`
- «измени вёрстку блока» → соответствующий `.astro`

---

## 6. Папка `src/content` — контент сайта

```text
src/content/
└── ru/
    └── *.json              ← текстовый контент страниц
```

Используется как **единый источник текстов**.

📌 Большинство текстовых правок делаются **только здесь**.

---

## 7. Папка `public` — статика

```text
public/
└── images/
    └── team/*.png          ← фото команды
```

Используются **root‑relative пути**:

```html
<img src="/images/team/photo.png" />
```

Это гарантирует корректную работу:
- на `localhost:4321`
- на `law.elegso.ru`
- после переезда на `elegso.ru`

---

## 8. Файлы управления смыслом

### `brand.md`
Содержит:
- тон коммуникации
- стиль текста
- визуальные правила

Любые изменения **тона бренда** → сначала правим `brand.md`.

### `STRUCTURE.md`
Содержит:
- архитектурные решения
- принципы построения сайта

Меняется **ТОЛЬКО при изменении логики проекта**.

### `site-map.json`
Содержит:
- карту страниц
- якоря меню

Меняется при:
- добавлении страниц
- изменении меню

---

## 9. Как работать с сайтом через запросы (рабочий процесс)

### Локальный цикл разработки

1. Правки выполняются **локально** на MacOS (MacBook Air, Тимченко Т.В.)
2. Тестирование: `http://localhost:4321`
3. Проверка: `npm run build` (обязательно без ошибок)
4. Коммит и push в GitHub: `elegso.site`
5. На сервере:
   - делается **полный бэкап текущего сайта** с датой и временем
   - сайт обновляется из GitHub

### Серверная инфраструктура

- **Гипервизор Proxmox**: 176.114.85.186
- **VM**: web-node (ID 710)
- **Внутренний IP**: 172.16.7.33
- **Публичный сайт**: law.elegso.ru

---

## 10. Обязательное правило обновления README

Если запрос:
- меняет структуру папок
- добавляет новые страницы или типы страниц
- меняет процесс деплоя
- вводит новые правила

👉 **README.md должен быть обновлён** и закоммичен.

---

## 11. Стандартный «промпт для ИИ» перед правкой

```
Цель правки: …
Где править: src/pages/… / src/components/… / src/styles/… / public/…
Ограничения:
- не трогать лишнее
- сохранить текущий стиль
- после правок npm run build обязан проходить без ошибок
```

Этот файл прикладывается в чат **каждый раз**, когда требуется вносить изменения.

---

## 12. Итог

Этот README:
- является паспортом проекта;
- описывает реальный рабочий процесс;
- позволяет управлять сайтом словами;
- обеспечивает безопасную работу ИИ с кодом.

📎 Рекомендуется всегда прикладывать этот файл в новый чат при работе с сайтом.
